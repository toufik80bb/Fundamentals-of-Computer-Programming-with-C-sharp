// Initialize a variable of type int with a value of 256 in hexadecimal format (256 is 100 in a numeral system with base 16).

using System;

namespace HexAssignment
{
	class HexAssignment
	{
		static void Main()
		{
			int HexValue = 0x100;
			Console.WriteLine(HexValue);
			Console.ReadLine();
		}
	}
}
